# Core web framework
Flask==3.0.2
Flask-CORS==4.0.0

# Audio processing
python-aifc  # Backport of aifc module for Python 3.13
SpeechRecognition>=3.10.1
pydub==0.25.1
gTTS==2.5.1
pygame
sounddevice==0.5.2  # Requires PortAudio system library

# AI and language processing
google-generativeai==0.3.2
python-dotenv==1.0.1

# Note: PyAudio is moved to optional-requirements.txt as it's only needed for local development
