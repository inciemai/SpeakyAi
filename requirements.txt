# Core web framework
Flask==3.0.2
Flask-CORS==4.0.0

# Audio processing
SpeechRecognition>=3.8.1,<3.11.0  # Older version for better compatibility
pydub==0.25.1
gTTS==2.5.1
pygame==2.5.2  # Specify exact version for stability

# AI and language processing
google-generativeai==0.3.2
python-dotenv==1.0.1

# Note: PyAudio is moved to optional-requirements.txt as it's only needed for local development
